using NeighborGraphs.GeometricPrimitives

# Ball Constructors -----------------------------------------------------
# Ball -> BV ------------------------------------------------------------
# `isContained(Ball, pt)` -----------------------------------------------
# `isContained(BV, Ball)` -----------------------------------------------
# `isContained(Ball, BV)` -----------------------------------------------
# `intersects(BV, Ball)` ------------------------------------------------
# `getReducedDimBall` ---------------------------------------------------
# `tightenBVBounds!` ----------------------------------------------------
# `getIntersection(BV, Ball)` -------------------------------------------